<!--To run the code, you will need a launch file that does two things:
    1. Loads the Panda URDF and SRDF onto the parameter server, and
    2. Puts the kinematics_solver configuration generated by the MoveIt Setup Assistant 
       onto the ROS parameter server in the namespace of the node that instantiates the 
       classes in this tutorial.-->
<launch>
  <include file="$(find irb1600_6_12_moveit_config)/launch/planning_context.launch">
    <arg name="load_robot_description" value="true"/>
  </include>

  <node name="robot_state"
        pkg="test_robot_state"
        type="robot_state"
        respawn="false" output="screen">
    <rosparam command="load"
              file="$(find irb1600_6_12_moveit_config)/config/kinematics.yaml"/>
  </node>
</launch>